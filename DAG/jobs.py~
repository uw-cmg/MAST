class JobA:

    self.jid
    self.sid
    self.out1
    self.out2
    def __init__(self, pids=[], cids=[]):
        '''CIDS is the list of childrent id. PIDS is the list of parents id
        '''
        self.cids = cids
        self.pids = pids
        self.is_complete = False

    def set_jid(jid):
        self.jid = jid

    def set_sid(sid):
        self.sid = sid

    def run():
        self.out1 = 2 
        self.out2 = 4 #dummy value which is not used in any children
        self.is_complete = True

    def update():
        for cid in self.cids:
            # load child from pickle manger or DAGScheduler 
        for child in self.children:
            
    
        
