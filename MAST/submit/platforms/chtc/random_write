#!/bin/env python
import os
import random
def main():
    randint = random.randint(0,2)
    rlist=["alpha","beta","gamma"]
    myfile = open("RANDOMWRITE","wb")
    myfile.write(rlist[randint] + "\n")
    myfile.close()

if __name__ == "__main__":
    main()
