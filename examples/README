To use these example files:
1. Go to the main directory
2. python initialize.py
3. The appropriate recipe files should all be in your ~/MAST/recipe_templates folder
4. Input the input file using the command:
mast -i xxx.inp
5. Start running mast using the command:
mast
